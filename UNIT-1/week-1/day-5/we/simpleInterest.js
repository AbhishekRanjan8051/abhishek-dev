var p = 100000; // This is Principal Amount
var r = 8; // This is Rate Percentage in Annum
var t = 5; // This is the Time in Year

/* 
The simple interest formula is 
(SI = p * r * t /100)
*/
var simpleInterest = (p * r * t) / 100;
console.log(simpleInterest);
